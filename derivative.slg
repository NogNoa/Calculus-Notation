prim {<operator> <derivative>}
form {a x <drivative>: <formula> a, x}
let {(a x <derivative>) \is <formula>}
form {a <drv-x> ?= a x <drivative>}
let {a b + <drv-x> = a <drv-x> b <drv-x> +}
let {a b * <drv-x> = a <drv-x> b * a b <drv-x> * + 

